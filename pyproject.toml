[project]
name = "sbscraper"
version = "0.1.0"
description = "Scrape all the things"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pdfplumber>=0.11.7",
    "playwright>=1.53.0",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0",
]
